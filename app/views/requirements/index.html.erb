<h1>需求列表</h1>

<div class="span12">
	<div class="product"
      	 <% if current_user.monthly_flag==1 %>
		style="display:none"
         <% end %>
		>
<%= link_to("新建需求",new_requirement_path, :class => "btn btn-mini btn-primary pull-right") %>
</div>

<table class="table">
	<thead>
		<tr>
			<td>#</td>
			<td>名称</td>
			<td>描述</td>
		</tr>
	</thead>

	<tbody>
		<% @requirements.each do|requirement| %>
		<tr>
			<td> # </td>
			<td> <%= link_to(requirement.name, requirement_path(requirement))%></td>
			<td> <%= requirement.description %> </td>
			<td> <%= link_to("编辑", edit_requirement_path(requirement), :class => "btn btn-mini")%>
				<%= link_to("删除", requirement_path(requirement), :class => "btn btn-mini", :method=>:delete,:confirm=>"确定要删除？")%>
			</td>
		</tr>

		<%end%>
	</tbody>
</table>
<div>


